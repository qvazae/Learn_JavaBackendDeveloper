<!-- prettier-ignore-start -->

# Описание решения

От общего к частному: чёткая sealed‑иерархия транспорта, понятные сущности и операции — без лишних параметров.

| 🧱 Иерархия (Sealed)                                      | 🚗 Виды транспорта                                                           | ⚡ Энергия и двигатели                                                             |
| --------------------------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Базы: `Transport`, `Engine` (наследники ограничены `permits`). | `Car`, `Airplane`, `Ship`, `Bicycle`, `ElectricCar` — все `final`.            | `CombustionEngine` (топливо: `GASOLINE`/`DIESEL`/`JET_FUEL`), `ElectricMotor` (кВт·ч). |
| Контракты: `EnginePowered` — для всего моторизованного.   | Немоторизованный: `Bicycle`. Моторизованные реализуют `EnginePowered`.       | Убрали «объём двигателя»: вводим только понятные параметры (л.с., кВт·ч, топливо). |

| 🧭 Поведение и операции                             | 🎛️ Консольный интерфейс                                                                 | 🔗 Соотношения (кратко)                                                                  |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| Общее для всех: `getMaxSpeed()`, `move()`, вывод инфо. | Меню: создание объектов всех типов, список, инфо, запуск/стоп двигателя, действие `move()`. | `Transport` → конкретные виды; `Engine` → `CombustionEngine`/`ElectricMotor`; `EnginePowered` ↔ моторизованные. |

# Диаграмма

Диаграмма строится из `transport_uml.puml`. Изображение `SVG` добавляется автоматически после изменений.

![Диаграмма классов](./transport_uml.svg)

<!-- prettier-ignore-end -->

