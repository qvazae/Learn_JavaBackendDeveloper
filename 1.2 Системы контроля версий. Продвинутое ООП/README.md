<!-- prettier-ignore-start -->

# Описание решения

Проект: компактная sealed‑иерархия транспорта + enum топлива. Есть консольное меню для быстрого создания и управления объектами.

| 🚗 Виды транспорта | 🧱 Архитектура | ⚙️ Двигатели |
| --- | --- | --- |
| `Car`, `Airplane`, `Ship`, `Bicycle` — все `final`. | Sealed‑базы: `Transport`, `Engine` с явным `permits`. | `CombustionEngine` (топливо: `GASOLINE`, `DIESEL`, `JET_FUEL`). |
| Поля: вместимость, двери/крыло/водоизмещение/передачи. | Интерфейс `EnginePowered` для техники с мотором. | `ElectricMotor` — задел для электротранспорта. |

| 🎛️ Консольный UI | 🚀 Запуск | 📁 Файлы |
| --- | --- | --- |
| Создание объектов, список, краткая инфо, старт/стоп двигателя, действие `move()`. | `cd "1.2 Системы контроля версий. Продвинутое ООП"` → `javac solution.java` → `java solution`. | Код+UI: `solution.java` • Диаграмма: `transport_uml.puml` • SVG: `transport_uml.svg`. |

# Диаграмма

Диаграмма строится из `transport_uml.puml`. В репозитории GitHub изображение `SVG` формируется автоматически после изменений.

![Диаграмма классов](./transport_uml.svg)

<!-- prettier-ignore-end -->
